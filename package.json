{
  "name": "label-generator",
  "version": "1.0.0",
  "description": "🏷️Bulk label generator with excel or csv file📦",
  "main": "server/app.js",
  "scripts": {
    "dev": "set NODE_ENV=development; concurrently --kill-others \"nodemon server/app.js\" \"npm run start --prefix client\"",
    "test": "node server/test.js",
    "deploy": "git checkout main && git pull && git merge develop && git push && git checkout develop"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geoje/LabelGenerator.git"
  },
  "author": "github.com/geoje",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/geoje/LabelGenerator/issues"
  },
  "homepage": "https://github.com/geoje/LabelGenerator#readme",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^7.5.0",
    "nodemon": "^2.0.20"
  }
}
